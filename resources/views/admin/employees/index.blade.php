@extends('layouts.admin')
@section('title','Employee')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h4 class="fw-bold mb-0"><i class="fas fa-user-tie me-2 text-primary"></i> Employee</h4><a href="{{ route('admin.employees.create') }}" class="btn btn-primary"><i class="fas fa-plus me-2"></i>Tambah</a></div>
<div class="card border-0 rounded-4 shadow-sm"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Nama</th><th>Email</th><th>Role</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($employees as $e)<tr><td class="fw-medium">{{ $e->name }}</td><td>{{ $e->email }}</td><td><span class="badge bg-{{ $e->role==='admin'?'danger'=>'info' }}-subtle">{{ $e->role }}</span></td><td><span class="badge bg-success-subtle text-success">{{ $e->status }}</span></td><td><a href="{{ route('admin.employees.edit',$e) }}" class="btn btn-sm btn-outline-primary"><i class="fas fa-edit"></i></a><form action="{{ route('admin.employees.destroy',$e) }}" method="POST" class="d-inline" onsubmit="return confirm('Hapus?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger ms-1"><i class="fas fa-trash"></i></button></form></td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted">Belum ada employee</td></tr>@endforelse</tbody></table></div>@if($employees->hasPages())<div class="p-3">{{ $employees->links() }}</div>@endif</div>
@endsection
